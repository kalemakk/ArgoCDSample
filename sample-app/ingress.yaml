apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: guestbook-ui-ingress
  namespace: users
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - guestbook-sybyl.centenarybank.demo.com
      secretName: tls-secret
  rules:
  - host: guestbook-sybyl.centenarybank.demo.com   
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: guestbook-ui
            port:
              number: 80